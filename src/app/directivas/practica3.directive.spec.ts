import { Practica3Directive } from './practica3.directive';

describe('Practica3Directive', () => {
  it('should create an instance', () => {
    const directive = new Practica3Directive();
    expect(directive).toBeTruthy();
  });
});
